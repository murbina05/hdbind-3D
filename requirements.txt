prolif>=2.0.0
# oddt removed — fails to build on Python 3.12 (import six in isolated build env); custom PLEC used instead
rdkit>=2023.3
mdanalysis>=2.5
deepchem>=2.8
scikit-learn>=1.3
scipy>=1.11
matplotlib>=3.7
seaborn>=0.12
pandas>=2.0
numpy>=1.24
xgboost>=2.0
torch>=2.3.0
joblib>=1.3
pytest>=7.4
# Optional — RAPIDS cuML for GPU-accelerated LogisticRegression
# Install separately: https://docs.rapids.ai/install
# cuml-cu12>=24.02
